#ifndef __MENU_H__
#define __MENU_H__
#include "cocos2d.h"
#include "Hero.h"
using namespace cocos2d;

class Menu
{
public:
	CREATE_FUC(Menu);
	bool init();
public:
	CCMenu* menu;
	Heor8 hero;
}

#endif
